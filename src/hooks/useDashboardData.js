/**
 * Alias del hook del Dashboard: el estado (invitaciones, analytics, mesas,
 * tareas) vive en `DashboardProvider`; este export mantiene el nombre del plan.
 */

export { useDashboard as useDashboardData } from "../components/dashboard/DashboardProvider"
